# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:
  - type: dnf
    install:
      install-setopt: tsflags=noscripts
      packages:
        - netbird
        - libappindicator-gtk3
        - libappindicator
        - netbird-ui
        - gnome-shell-extension-appindicator
        
